# 🥗 AI 다이어트 & 웰니스 코칭 플랫폼 — 종합 발표 대본 및 기술 학습 가이드 (Master Study Guide)

본 문서는 **15장 발표용 PPT ([AI_Diet_Coach_Presentation.pptx](file:///Users/a0000/Dev/%EB%A9%94%ED%83%80%EC%BD%94%EB%93%9CM/%EB%9D%BC%EC%9D%B4%EB%B8%8C%20%EC%8A%A4%ED%84%B0%EB%94%94/Project/AI_Diet_Coach_Presentation.pptx))**를 바탕으로, 발표자가 슬라이드별 핵심 전달 메시지, 구어체 발표 대본, 심층 기술 원리, 예상 질문(Q&A) 방어 전략을 완벽히 숙지할 수 있도록 제작된 마스터 가이드입니다.

---

## 📌 목차 (Index)
- **1. 프로젝트 1분 엘리베이터 피치 (Core Value)**
- **2. 슬라이드별 완벽 발표 대본 & 핵심 전달 포인트 (Slide 01 ~ 15)**
- **3. 핵심 기술 심층 마스터 (Deep Tech Knowledge)**
- **4. 평가위원/청중 예상 Q&A 10선 및 모범 답변**

---

## 1. 프로젝트 1분 엘리베이터 피치 (Elevator Pitch)
"기존 다이어트 앱은 매 끼니마다 음식 그램 수를 검색하고 수동으로 타이핑해야 하는 극심한 번거로움이 있었고, 일반 생성형 AI는 칼로리와 영양 수치를 마음대로 지어내는 치명적인 **환각(Hallucination)** 문제가 있었습니다.

저희 **'AI 다이어트 & 웰니스 코치'**는 식단 사진 1장만 업로드하면 **멀티모달 Gemini 모델**이 메뉴를 자동 인식하고, **식약처 표준 영양 DB 도구(Function Calling)**를 강제로 호출하여 100% 신뢰할 수 있는 수치를 기반으로 맞춤형 코칭을 제공합니다. 또한 **503 과부하/429 속도제한을 0.5초 만에 복구하는 5대 Flash 모델 무중단 폴백**, **모듈화된 클린 아키텍처**, **LangGraph 기반 의도 분류 라우팅**, **7차시 Self-RAG 3단계 품질 게이트**, **ACSM 표준 METs 운동 소모 칼로리 계산**, **Human-in-the-Loop 스마트 저장**, 그리고 매월 1일 개인 메신저로 리포트를 쏴주는 **텔레그램 결산 자동화**까지 결합한 **실제 동작하는 풀스택 프로덕션 서비스**입니다."

---

## 2. 슬라이드별 완벽 발표 대본 & 핵심 전달 포인트

### Slide 01. 표지 (Cover)
- **핵심 키워드**: 풀스택 AI 에이전트, 식약처 표준 DB, LangGraph, Self-RAG
- **발표 대본**:
  "안녕하십니까. 메타코드M 라이브스터디 1~7차시의 핵심 AI 이론과 프레임워크를 실전 풀스택 서비스로 완성한 **'AI 다이어트 & 웰니스 코칭 플랫폼'** 발표를 맡은 [발표자 이름]입니다. 저희 프로젝트는 단순한 챗봇 튜토리얼을 넘어, 공공데이터베이스 도구 바인딩과 3단계 품질 검증, 그리고 외부 메신저 결산 자동화까지 연결된 완성형 서비스입니다. 지금부터 발표를 시작하겠습니다."

---

### Slide 02. 목차 (Table of Contents)
- **핵심 키워드**: 4대 대단원, 문제 정의 ➔ 트러블슈팅 & 아키텍처 ➔ 기능 실측 ➔ 강의 연계
- **발표 대본**:
  "오늘 발표는 크게 4개의 파트로 진행됩니다. 첫째, 기존 서비스들의 한계와 기획 배경. 둘째, 프로젝트 아키텍처 및 503 과부하 트러블슈팅 해결 과정. 셋째, Self-RAG 품질 게이트와 BMR 개인화 등 핵심 기능 실측 시연. 마지막으로 1~7차시 강의 커리큘럼과의 연계 의의 및 확장 계획 순으로 말씀드리겠습니다."

---

### Slide 03. 기획 배경 및 문제 정의 (Problem & Solution)
- **핵심 키워드**: 수동 기록 피로도, LLM 환각(Hallucination), 멀티모달 인식, 공공데이터 강제 바인딩
- **발표 대본**:
  "다이어트의 가장 큰 적은 '기록의 피로도'입니다. 음식마다 몇 그램인지 일일이 검색해 넣는 작업은 작심삼일로 끝나기 일쑤였습니다. 그렇다고 일반 ChatGPT에 물어보면 떡볶이 칼로리를 200kcal라고 지어내는 등 **의료/영양 도메인에서 치명적인 환각**이 발생합니다.
  저희는 이 문제를 두 가지 기술로 해결했습니다. 사진 1장으로 메뉴를 자동 식별하는 **멀티모달 비전 AI**, 그리고 영양 수치를 추측하지 않고 파이썬 도구를 통해 식약처 CSV DB 실측치만을 가져오도록 강제하는 **Function Calling 도구 바인딩**입니다."

---

### Slide 04. 전체 풀스택 시스템 아키텍처 (System Architecture)
- **핵심 키워드**: Streamlit 반응형 웹, Gemini Flash 엔진, SQLite 암호화 DB, Telegram Bot
- **발표 대본**:
  "시스템 아키텍처는 보시는 바와 같이 프론트엔드, AI 엔진, 데이터베이스, 외부 자동화 서비스의 4계층으로 유기적으로 연결되어 있습니다.
  사용자는 Streamlit 반응형 인터페이스를 통해 사진과 메시지를 전달하고, Gemini Flash 기반 에이전트가 3대 전문 도구를 호출합니다. 모든 데이터는 SHA-256 단방향 솔트 암호화가 적용된 SQLite DB에 안전하게 격리 저장되며, 백그라운드 스케줄러를 통해 매월 1일 텔레그램으로 월간 결산이 자동 발송됩니다."

---

### Slide 05. 프로젝트 폴더 및 파일 아키텍처 (Project Structure)
- **핵심 키워드**: Clean Architecture, 모듈화, 네임스페이스 격리, 클라우드 무결점 배포
- **발표 대본**:
  "보시는 화면은 실제 서비스 구현에 사용된 **프로젝트 디렉터리 및 파일 아키텍처**입니다.
  저희는 코드의 유지보수성과 확장성을 극대화하기 위해 **Clean Architecture 원칙**을 적용했습니다.
  메인 웹 UI는 `app.py`에 집중시키고, AI 에이전트는 `ai_agent/`에, 3대 전문 도구는 `app_tools/`, 데이터베이스와 보안 로직은 `app_db/`, 외부 메신저 연동은 `app_services/`로 철저히 모듈화했습니다.
  특히 리눅스 클라우드 서버 환경에서 발생할 수 있는 전역 패키지 이름 충돌을 원천 차단하기 위해 고유 네임스페이스를 구축하여, 안정적인 CI/CD 배포가 가능하도록 완성했습니다."

---

### Slide 06. [신규 트러블슈팅] 503 과부하/429 제한 방어 및 복원력 구축
- **핵심 키워드**: 503 UNAVAILABLE, 429 속도제한, 5대 Flash 모델 다중 폴백, 무중단 자동 전환
- **발표 대본**:
  "실제 서비스 개발 및 배포 과정에서 직면했던 가장 치명적인 문제는 **사진 식단 분석 시 구글 서버의 트래픽 폭주로 인한 `503 UNAVAILABLE` 과부하 에러**와 **무료 한도 초과 `429` 에러**였습니다.
  단일 모델에 의존하면 사용자의 서비스가 완전히 멈추게 됩니다.
  이를 해결하기 위해 저희는 **5대 Flash 모델 간의 0.5초 무중단 자동 폴백(Multi-tier Cascading Fallback) 파이프라인**을 구축했습니다.
  우측 실제 실행 로그를 보시면, 1순위 모델에서 503 과부하가 감지되는 즉시 시스템이 2순위 `gemini-3.7-flash`로 자동 전환하여 100% 정상 응답을 복구해 낸 것을 확인하실 수 있습니다."

---

### Slide 07. LangGraph 기반 조건부 라우팅 (Agent Workflow)
- **핵심 키워드**: StateGraph, Intent Router, 4대 조건부 분기, Human-in-the-Loop
- **발표 대본**:
  "에이전트의 내부 흐름은 **LangGraph의 조건부 분기(Conditional Routing)** 패턴으로 설계되었습니다.
  사용자의 입력이 들어오면 `Intent Router`가 의도를 분석합니다. 식단 분석이면 `food_handler`, 운동 기록이면 `exercise_handler`, 다이어트 상식 문의면 `rag_handler`, 일상 대화면 `general_handler`로 분기합니다. 이후 분석 결과는 응답 하단에 `Human-in-the-Loop` 스마트 카드로 렌더링되어, 사용자가 원클릭으로 검토 및 승인할 때만 DB에 안전하게 저장됩니다."

---

### Slide 08. [핵심] Self-RAG 3단계 품질 게이트 (LLM-as-a-Judge)
- **핵심 키워드**: 7차시 프로젝트 패턴, 이진 평가(Yes/No), 환각 검출, 임상 가드레일
- **발표 대본**:
  "7차시에서 학습한 **Self-RAG 3단계 품질 게이트**를 우리 영양 도메인에 완벽히 이식했습니다.
  우측 터미널 로그를 보시면, 에이전트가 답변을 생성하기 전 **LLM-as-a-Judge**가 3단계를 이진 평가합니다.
  1단계: 사용자가 말한 식단과 DB 조회 데이터가 실제로 일치하는지 평가(`Relevance Check`).
  2단계: 생성된 답변의 칼로리와 탄단지가 DB 실측치와 100% 일치하는지 환각 검출(`Grounding Check`).
  3단계: 극단적 초저열량 경고 및 의학적 면책 문구가 포함되었는지 가드레일 검증(`Safety Check`).
  이 3단계를 모두 통과한 안전한 답변만이 최종적으로 사용자에게 전달됩니다."

---

### Slide 09. 핵심 기술 ① : 멀티모달 & Function Calling 영양 실측
- **핵심 키워드**: 식약처 5,000+ CSV, 3단계 프롬프트(요약-진단-추천), 메타데이터 파싱
- **발표 대본**:
  "실제 도구 실행 로그를 보시면, '닭가슴살 샐러드'라는 입력에 대해 모델이 `search_food_nutrition()` 함수를 자동 호출하여 식약처 DB의 실측치인 135kcal, 단백질 26g, 나트륨 58mg을 정확히 반환했습니다.
  코칭 프롬프트는 '1단계 영양소 요약 ➔ 2단계 일일 목표 대비 진단 ➔ 3단계 다음 식사 추천 메뉴'의 3단계 구조로 구성되어 체계적인 솔루션을 제공하며, 응답 맨 끝에 `<!-- MEAL_DATA -->` JSON 태그를 자동으로 생성합니다."

---

### Slide 10. 핵심 기술 ② : METs 운동 계산 & 영양 RAG & 5대 모델 폴백
- **핵심 키워드**: ACSM METs 공식, 순 칼로리(Net Calories), Flash 5대 모델 무중단 전환
- **발표 대본**:
  "섭취 칼로리뿐 아니라 운동 소모 칼로리까지 종합 관리합니다. 미국 스포츠의학회(ACSM) 공식인 `1.05 × METs × 체중 × 시간`을 적용하여, 70kg 사용자가 러닝 30분을 수행했을 때 정확히 312.4 kcal 소모를 도출합니다.
  또한 영양 RAG 지식 검색을 통해 혈당 스파이크 방지 및 정체기 극복 가이드를 과학적으로 제공합니다."

---

### Slide 11. 핵심 기술 ③ : 신체 맞춤 영양 추천 & 보안 DB
- **핵심 키워드**: Mifflin-St Jeor 공식, BMR/TDEE, SHA-256 + Salt 보안 암호화
- **발표 대본**:
  "사용자 맞춤 영양 설정을 위해 임상의학에서 가장 정확도가 높은 **미플린-세인트지올 공식**을 도입했습니다. 사용자의 성별, 나이, 키, 체중, 활동 계수를 계산하여 기초대사량과 활동대사량을 산출하고, 다이어트 목적에 맞게 일일 목표 칼로리와 단백질 권장량을 자동 세팅합니다. 모든 비밀번호는 솔트(Salt)를 가미한 단방향 SHA-256 해싱으로 암호화되어 로컬 DB에서도 완벽한 보안성을 자랑합니다."

---

### Slide 12. 핵심 기능 ④ : Human-in-the-Loop 스마트 저장 & 실시간 통계
- **핵심 키워드**: 원클릭 DB 저장 카드, Net Calories(순 칼로리), Plotly 인터랙티브 차트
- **발표 대본**:
  "스마트 저장 기능 덕분에 사용자는 AI가 분석한 결과를 보고 [이 식단 DB에 바로 저장] 버튼만 누르면 1초 만에 SQLite에 기록됩니다.
  우측 실제 쿼리 결과를 보시면, 당일 섭취한 520kcal에서 운동으로 소모한 312.4kcal가 차감된 **순 칼로리(Net Calories) 207.6kcal**가 실시간 집계되어 일별 게이지 및 도넛 차트에 즉각 반영됩니다."

---

### Slide 13. 핵심 기능 ⑤ : 텔레그램 연동 및 월간 결산 자동화
- **핵심 키워드**: APScheduler, 매월 1일 브로드캐스트, Matplotlib 차트 자동 생성
- **발표 대본**:
  "다이어트의 지속성을 위해 텔레그램 봇 자동화 파이프라인을 구축했습니다. 백그라운드 스케줄러가 매월 1일 자정에 가동되어, 한 달간의 총 칼로리, 목표 달성 성공률, 최다 섭취 음식 TOP 3, 그리고 Matplotlib으로 렌더링한 칼로리 추이 그래프 이미지를 사용자의 텔레그램으로 자동 브로드캐스트합니다."

---

### Slide 14. 강의 커리큘럼(1~7차시) 연계 및 의의 (Course Mapping)
- **핵심 키워드**: 1~7차시 집대성, 프롬프트-RAG-Tool-LangGraph-Self RAG 풀스택
- **발표 대본**:
  "본 프로젝트는 1차시 프롬프트 엔지니어링부터 2~4차시 데이터 전처리 및 RAG 검색, 5차시 도구 호출, 6차시 세션 상태 관리, 7차시 Self-RAG 품질 게이트 및 조건부 라우팅까지 **라이브스터디 전 차시의 핵심 이론을 하나의 완성된 상용 수준 웹 서비스로 집대성**했다는 점에서 기술적 의의를 갖습니다."

---

### Slide 15. 배포 성과 및 향후 발전 로드맵 (Summary & Roadmap)
- **핵심 키워드**: GitHub 오픈소스, Streamlit Community Cloud 글로벌 배포, 애플워치/CGM 확장
- **발표 대본**:
  "현재 본 서비스는 GitHub 오픈소스 저장소와 Streamlit Community Cloud를 통해 전 세계 어디서나 모바일과 PC로 접속 가능한 상태로 배포 완료되었습니다. 향후 애플워치/갤럭시워치의 헬스 데이터 실시간 연동과 연속 혈당 측정기(CGM) 데이터 접목을 통해 차세대 초개인화 헬스케어 플랫폼으로 확장해 나갈 계획입니다. 경청해 주셔서 대단히 감사합니다."

---

## 3. 핵심 기술 심층 마스터 (Deep Tech Knowledge)

### ① 503 UNAVAILABLE 및 429 속도제한 무중단 폴백(Fallback) 메커니즘
- **문제**: 구글 Gemini API 서버는 특정 모델에 트래픽이 몰리면 `503 UNAVAILABLE`을 반환하며, Pro 모델은 무료 티어에서 분당 2회(2 RPM)만 호출해도 `429 RESOURCE_EXHAUSTED`가 발생합니다.
- **해결 알고리즘**:
  ```python
  CANDIDATE_MODELS = [
      "gemini-3.6-flash",    # 1순위 (초고속 도구 바인딩)
      "gemini-3.7-flash",    # 2순위 (차세대 플래시)
      "gemini-3.5-flash",    # 3순위 (고가용성 플래시)
      "gemini-flash-latest", # 4순위 (최신 안정화 에일리어스)
      "gemini-2.5-flash-lite"# 5순위 (초경량 최저 토큰 소모)
  ]
  # 503/429 에러 감지 시 0.5초 내 순차적으로 모델 세션을 재초기화하여 자동 재시도
  ```

### ② Clean Architecture 폴더 구조의 장점
- `ai_agent/`, `app_tools/`, `app_db/`, `app_services/`의 4대 패키지 분리를 통해 단일 책임 원칙(SRP)을 준수하고, 단위 테스트 및 기능 추가 시 다른 계층에 영향을 주지 않는 완벽한 독립성을 확보했습니다.

### ③ Function Calling (도구 바인딩) 원리
- **동작 방식**: LLM에게 직접 영양 성분을 답변하게 하지 않고, 파이썬 함수 `search_food_nutrition(food_name)`의 스키마를 JSON 형태로 모델에 바인딩합니다.
- **장점**: 모델이 답변을 지어내지 않고 파이썬 인터프리터가 식약처 CSV 파일(`merged_food_nutrition.csv`)에서 정확한 행을 탐색하여 칼로리/영양소를 반환하므로 환각율이 0%입니다.

### ④ Self-RAG 3단계 품질 게이트 (LLM-as-a-Judge)
1. **`grade_nutrition_docs`**: 사용자 질문 ↔ DB 검색 결과 간의 관련성을 Pydantic 기반 Structured Output(`BinaryGradeNutrition`)으로 이진 평가.
2. **`check_hallucination_nutrition`**: 조회된 DB 수치(grounding facts)와 모델 생성 텍스트의 수치가 일치하는지 검증. 불일치 시 재생성 루프 트리거.
3. **`check_clinical_safety`**: 섭취 칼로리가 극단적으로 낮거나(예: 일 500kcal 미만) 편향된 식단일 경우 거식증/영양실조 위험 경고와 의학적 면책 문구 강제 삽입.

### ⑤ Mifflin-St Jeor 기초대사량(BMR) & 활동대사량(TDEE) 공식
- **남성 BMR**: `(10 × 체중kg) + (6.25 × 키cm) - (5 × 나이) + 5`
- **여성 BMR**: `(10 × 체중kg) + (6.25 × 키cm) - (5 × 나이) - 161`
- **TDEE**: `BMR × 활동계수(1.2 ~ 1.725)`
- **다이어트 목표 칼로리**: `TDEE - 450 kcal` (주당 약 0.5kg 체지방 감량 표준)

### ⑥ ACSM 표준 METs 운동 소모 칼로리 공식
- **공식**: `소모 칼로리(kcal) = 1.05 × METs × 체중(kg) × 운동시간(hr)`
- **주요 METs 계수**: 러닝(8.5), 웨이트 트레이닝(5.5), 수영(7.5), 줄넘기(10.0), 자전거(7.0), 걷기(3.5)

---

## 4. 평가위원/청중 예상 Q&A 10선 및 모범 답변

#### Q1. 음식 사진을 올렸을 때 503 과부하나 429 속도제한 에러가 발생했던 문제는 어떻게 해결했나요?
> **답변**: "단일 모델에 의존하면 구글 서버의 일시적 트래픽 스파이크(`503`)나 Pro 모델의 좁은 무료 한도(`429`)로 인해 서비스가 멈춥니다. 저희는 `ai_agent/diet_agent.py`에 **5대 Flash 모델 간 캐스케이딩 폴백(Cascading Fallback) 파이프라인**을 구축하여, 503이나 429 에러가 감지되는 즉시 0.5초 이내에 예비 Flash 모델로 자동 전환하여 100% 무중단 응답을 복구해 내도록 설계했습니다."

#### Q2. 일반 ChatGPT나 Claude에 식단을 물어보는 것과 이 서비스의 결정적 차이는 무엇인가요?
> **답변**: "일반 챗봇은 내부 파라미터 메모리에 의존하여 그럴듯한 칼로리를 지어내는 **환각(Hallucination)**이 발생합니다. 반면 저희 시스템은 **식약처 공공데이터 5,000+ 식품 DB를 Function Calling으로 강제 바인딩**하고 **Self-RAG 3단계 품질 게이트**를 거치기 때문에, 100% 검증된 공인 영양 수치만을 기반으로 코칭을 제공합니다."

#### Q3. Self-RAG 품질 게이트를 매번 거치면 응답 속도(Latency)와 토큰 비용이 늘어나지 않나요?
> **답변**: "이를 해결하기 위해 품질 검증 전용 평가 모델을 초고속 경량 모델인 `gemini-2.5-flash-lite`로 분리하고, JSON Structured Output을 적용하여 단 1~2개의 토큰(Yes/No)만 생성하도록 최적화하여 0.3초 이내에 평가가 완료되도록 경량화했습니다."

#### Q4. 식약처 DB에 등록되지 않은 희귀 음식이나 해외 식품은 어떻게 처리하나요?
> **답변**: "식약처 DB 검색 결과가 없거나 관련성 평가에서 `No` 판정을 받으면, **Corrective RAG(CRAG) 보정 루프**가 가동되어 영양 백과 지식 검색과 일반 지식 베이스를 통해 유사 대체 식재료의 영양 성분을 추정하고, 사용자에게 '표준 추정치'임을 안내합니다."

#### Q5. Human-in-the-Loop(스마트 저장)을 도입한 구체적인 이유는 무엇인가요?
> **답변**: "AI가 분석한 결과를 무조건 DB에 강제 저장하면, 사용자가 음식의 양(g)을 다르게 먹었거나 단순 질문만 한 경우에도 잘못된 데이터가 누적됩니다. 따라서 AI 응답 하단에 감지된 식단 카드를 띄우고 사용자가 직접 확인 후 원클릭 승인할 때만 저장하도록 하여 데이터 무결성을 보장했습니다."

#### Q6. 개인정보 및 비밀번호는 어떻게 보호되고 있나요?
> **답변**: "단순 텍스트 저장이 아닌, 사용자마다 고유한 32바이트 난수 솔트(Salt)를 생성하여 비밀번호와 결합한 뒤 **SHA-256 단방향 암호화 해싱**을 적용했습니다. DB 파일이 유출되더라도 레인보우 테이블 공격으로부터 안전합니다."

#### Q7. 순 칼로리(Net Calories) 지표가 왜 중요한가요?
> **답변**: "단순히 섭취 칼로리만 보면 운동을 열심히 한 날에도 과식했다고 오해할 수 있습니다. `순 칼로리 = 섭취량 - 운동 소모량`을 실시간 계산하여 대시보드 게이지에 반영함으로써, 운동을 통해 획득한 칼로리 버퍼를 직관적으로 확인하고 지속 가능한 다이어트 동기부여를 제공합니다."

#### Q8. 텔레그램 월간 결산 자동화는 어떤 원리로 실행되나요?
> **답변**: "`scheduler.py`의 APScheduler 백그라운드 데몬이 매월 1일 자정에 트리거됩니다. 텔레그램 Chat ID가 등록된 유저들의 전월 식단 데이터를 SQLite에서 집계하고, Matplotlib으로 칼로리 추이 그래프 이미지를 메모리 버퍼에서 즉시 생성하여 텔레그램 Bot API로 일괄 브로드캐스트합니다."

#### Q9. 다이어트 정체기나 혈당 스파이크 질문은 어떻게 처리하나요?
> **답변**: "`app_tools/nutrition_rag.py`에 내장된 임상 영양 백과 지식 베이스를 통해, 혈당 스파이크 방지를 위한 '식이섬유 ➔ 단백질 ➔ 탄수화물' 섭취 순서 가이드와 정체기 극복을 위한 '리피드(Refeed) 전략'을 전문적으로 안내합니다."

#### Q10. 이 프로젝트를 상용 서비스로 출시할 때 다음 단계 과제는 무엇인가요?
> **답변**: "애플 헬스키트(HealthKit) 및 삼성 헬스 SDK와 연동하여 사용자의 스마트워치 걸음 수와 활동 칼로리를 백그라운드에서 실시간 동기화하고, 연속 혈당 측정기(CGM) 데이터를 접목하여 식후 혈당 반응 기반의 초개인화 AI 코칭으로 발전시키는 것입니다."
